# 运行依赖（版本锁定，审计 M6：CI/本地一致、构建可复现）
fastapi==0.141.1
uvicorn==0.52.1
httpx[http2]==0.28.1
# 抖音 web API 请求（impersonate="chrome" 伪造浏览器 TLS 指纹，绕过 Janus 风控空 body）
curl_cffi==0.16.2
# WebP 解码（动画 WebP 帧提取，FFmpeg 原生解码器不支持动画）
Pillow==12.3.0
